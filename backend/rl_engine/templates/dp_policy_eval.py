# ---------------------------------------------
# Dynamic Programming: Policy Evaluation
# ---------------------------------------------
# You must implement the policy evaluation step.
# `V` is the value state array.
# `policy` is the current policy table.
# `env` has properties like env.P[state][action] which gives (prob, next_state, reward, done)

def policy_evaluation(V, policy, env, gamma=0.9, theta=1e-8):
    # TODO: Implement the iterative policy evaluation loop.
    # Write your algorithmic implementation below.
    pass
